export function POCBanner() {
  return (
    <div className="bg-warning/10 border-b border-warning/30 text-warning text-center text-xs py-1.5 font-medium tracking-wide">
      ⚡ POC Mode — API calls are simulated with mock data
    </div>
  );
}
